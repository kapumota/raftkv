FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod ./
COPY cmd ./cmd
COPY internal ./internal
RUN go build -o /out/client-driver ./cmd/client-driver

FROM alpine:3.20
COPY --from=build /out/client-driver /usr/local/bin/client-driver
ENTRYPOINT ["/usr/local/bin/client-driver"]
